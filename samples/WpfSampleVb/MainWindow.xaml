<Window x:Class="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="계산기 샘플" Width="300" Height="240" WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="A" Width="40" VerticalAlignment="Center"/>
            <TextBox x:Name="txtA" AutomationProperties.AutomationId="txtA" Width="200"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="B" Width="40" VerticalAlignment="Center"/>
            <TextBox x:Name="txtB" AutomationProperties.AutomationId="txtB" Width="200"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,0,0,8">
            <Button x:Name="btnAdd" AutomationProperties.AutomationId="btnAdd" Content="더하기" Width="80" Click="OnAdd"/>
            <Button x:Name="btnSub" AutomationProperties.AutomationId="btnSub" Content="빼기" Width="80" Margin="8,0,0,0" Click="OnSub"/>
        </StackPanel>
        <TextBlock x:Name="lblResult" AutomationProperties.AutomationId="lblResult" Grid.Row="3" FontSize="18"/>
    </Grid>
</Window>
